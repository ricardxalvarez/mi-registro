import Joi from "joi";

export const register = {
    // name: Joi.string().required().max(40),
    // username: Joi.string().min(4).pattern(/^[a-zA-Z0-9_.-]+$/).required(),
    // lastName: Joi.string().required().max(40)
    // password: Joi.string().min(8)
    // identification: str
    // pic: str = None
    // gender: str
    // email: EmailStr
    // age: int
    // nationality: str
    // civil_status: str
    // address: str
    // # academic
    // years_of_service: int = None
    // expertise: str = None
    // degree: str = None
    // current_studies: str = None
    // # labor
    // rank: str
}